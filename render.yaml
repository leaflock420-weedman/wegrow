# WeGrow by LeafLock — static site for wegrow.leaflock.com.au
services:
  - type: web
    name: wegrow
    runtime: static
    plan: free
    repo: https://github.com/leaflock420-weedman/wegrow
    branch: main
    buildCommand: "echo build complete"
    staticPublishPath: .
    domains:
      - wegrow.leaflock.com.au